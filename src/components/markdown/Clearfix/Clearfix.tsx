import { FunctionComponent } from 'react';
import styled from 'styled-components';

const ClearFix = styled.div`
  clear: both;
`;

const Clearfix: FunctionComponent = () => <ClearFix />;

export default Clearfix;
